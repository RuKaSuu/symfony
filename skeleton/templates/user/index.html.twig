{% extends 'base.html.twig' %}
{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { display:flex; justify-content: center; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #FFFFFF; padding: 2px 6px; }

</style>

<div class="example-wrapper">
    <h1>Create User</h1>
</div>
{#    Render the form in UserController#}

{#    {{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#    {{ form_end(form) }}#}
        <div class="page-wrapper bg-dark p-t-100 p-b-50">
            <div class="wrapper wrapper--w900">
                {{ form_start(form) }}
                <div class="card card-6">
                    <div class="card-heading">
                        <h2 class="title">Apply for job</h2>
                    </div>
                    <div class="card-body">
                        <form>
                            <div class="form-row">
                                <div class="name">First name</div>
                                <div class="value">
                                {{ form_row(form.name) }}
                                </div>
                            </div>
                            <div class="form-row">
                                {{ form_row(form.surname) }}
                            </div>
                            <div class="form-row">
                                {{ form_row(form.age) }}
                            </div>
                            <div class="form-row">
                                {{ form_row(form.address) }}
                            </div>
                            <div class="form-row">
                                {{ form_row(form.levels) }}
                            <div class="form-row">
                                    <div class="input-group js-input-file">
                                        <label class="label--file" for="file">Copypast url img</label>
                                        <input class="input-file"  type="text" image="image" id="image">
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="card-footer">
                        <btn class="btn-primary">
                            {{ form_row(form.save) }}
                        </btn>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>

{#    <form action="{{ controller(#}
{#        'App\\Controller\\UserController::createUser',#}
{#        {'name': $_POST['name'] , 'surname': $_POST['surname'],'age': $_POST['age'], 'address' : $_POST['address'], 'levels' : $_POST['levels']})#}
{#    }}" method="post">#}
{#        <input type="text" placeholder="name">#}
{#        <input type="text" placeholder="surname">#}
{#        <input type="text" placeholder="age">#}
{#        <input type="text" placeholder="address">#}
{#        <input type="text" placeholder="levels">#}
{#        <input type="submit" value="Send">#}
{#    </form>#}

{#    <code>{{ absolute_url(path('user')) }}</code>#}
{#    <code>{{ render(controller(#}
{#            'App\\Controller\\UserController::createUser',#}
{#            {'name': 'dede', 'surname': 'deded', 'age' : 19 , 'address' : '14 rue du polisson', 'levels': 1}#}
{#        )) }}</code>#}

{#    <ul>#}
{#        <li>Your controller at <code><a href="{{ 'C:/Users/lucas/OneDrive/Bureau/Git/symfony/skeleton/src/Controller/UserController.php'|file_link(0) }}">src/Controller/UserController.php</a></code></li>#}
{#        <li>Your template at <code><a href="{{ 'C:/Users/lucas/OneDrive/Bureau/Git/symfony/skeleton/templates/user/index.html.twig'|file_link(0) }}">templates/user/index.html.twig</a></code></li>#}
{#    </ul>#}
</div>
{% endblock %}
